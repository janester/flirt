<% if session[:user_id].present?%>
$("#form").empty().hide();
$("#login_link").remove();
var a = $("<a>").attr("href", "#")
a.text("Log Out").addClass("button radius")
$("body").prepend(a)
<% else %>
$("#form").empty().show().append("<%=j render :partial => 'session/form' %>");
<% end %>
